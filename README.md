в env файле нужны такие переменные, как postgres_db postgres_user postgres_password postgres_host postgres_port django_secret_key Не знаю как на других системах, но на моём ноутбуке с 16 гб оперативы и i5 12450h после поднятия контейнера со всем проектом(в котором вложены все отдельные) onlyoffice запускается около минуту, и может слегка убивать сайт Сайт уже развёрнут с помощью nginx А ещё изменения с онлиофис на диск также длится где-то секунд 8 С МОМЕНТА ЗАКРЫТИЯ ВКЛАДКИ С ДОКУМЕНТОМ
